services:
  - type: web
    name: nova-haidong-quiz
    env: node
    buildCommand: npm install
    startCommand: node server.js
    plan: free
    healthCheckPath: /
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: GH_TOKEN
        sync: false
      - key: GH_REPO
        value: FGboss/nova-haidong-quiz